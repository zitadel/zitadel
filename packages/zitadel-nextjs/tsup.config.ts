import { defineConfig, Options } from "tsup";

export default defineConfig((options: Options) => ({
  entry: {
    index: "src/index.ts",
    "auth/index": "src/auth/index.ts",
    "auth/oidc": "src/auth/oidc.ts",
    "auth/session": "src/auth/session.ts",
    "auth/bearer-token": "src/api/bearer-token.ts",
    cli: "src/cli/index.ts",
    middleware: "src/middleware.ts",
    "server-action": "src/server-action.ts",
    api: "src/api.ts",
    "actions/webhook": "src/webhook.ts",
  },
  format: ["esm", "cjs"],
  dts: true,
  splitting: true,
  sourcemap: true,
  clean: true,
  treeshake: true,
  external: [
    "next",
    "next/headers",
    "next/navigation",
    "next/server",
    "react",
    "react-dom",
    "@zitadel/zitadel-js",
    "@zitadel/zitadel-js/auth/oidc",
    "@zitadel/zitadel-js/auth/session",
    "@zitadel/zitadel-js/auth/bearer-token",
    "@zitadel/zitadel-js/api/v2",
    "@zitadel/zitadel-js/actions/webhook",
    "@zitadel/zitadel-js/node",
    "@zitadel/react",
  ],
  ...options,
}));

{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "check": {},
    "build": {
      "inputs": ["default"],
      "outputs": ["{projectRoot}/dist/**"],
      "dependsOn": ["^generate"]
    }
  }
}

{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@zitadel/proto",
  "targets": {
    "generate": {
      "inputs": [
        "default",
        "{workspaceRoot}/pnpm-lock.yaml",
        "{workspaceRoot}/proto/**/*"
      ],
      "outputs": [
        "{projectRoot}/cjs",
        "{projectRoot}/es",
        "{projectRoot}/types"
      ]
    }
  }
}
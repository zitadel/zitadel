{
    "name": "@zitadel/common",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/zitadel-common/src",
    "targets": {
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo 'linting common'"
            }
        }
    }
}
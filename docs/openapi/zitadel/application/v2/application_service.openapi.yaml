openapi: 3.1.0
info:
  title: zitadel.application.v2
paths:
  /zitadel.application.v2.ApplicationService/CreateApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: CreateApplication
      description: |-
        Create Application

         Create an application. The application can be OIDC, API or SAML type, based on the input.

         Required permissions:
           - project.app.write
      operationId: zitadel.application.v2.ApplicationService.CreateApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.CreateApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.CreateApplicationResponse'
  /zitadel.application.v2.ApplicationService/UpdateApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: UpdateApplication
      description: |-
        Update Application

         Changes the configuration of an OIDC, API or SAML type application, as well as
         the application name, based on the input provided.

         Required permissions:
           - project.app.write
      operationId: zitadel.application.v2.ApplicationService.UpdateApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.UpdateApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.UpdateApplicationResponse'
  /zitadel.application.v2.ApplicationService/GetApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: GetApplication
      description: |-
        Get Application

         Retrieves the application matching the provided ID.

         Required permissions:
           - project.app.read
      operationId: zitadel.application.v2.ApplicationService.GetApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.GetApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.GetApplicationResponse'
  /zitadel.application.v2.ApplicationService/DeleteApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: DeleteApplication
      description: |-
        Delete Application

         Deletes the application belonging to the input project and matching the provided
         application ID.

         Required permissions:
           - project.app.delete
      operationId: zitadel.application.v2.ApplicationService.DeleteApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.DeleteApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.DeleteApplicationResponse'
  /zitadel.application.v2.ApplicationService/DeactivateApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: DeactivateApplication
      description: |-
        Deactivate Application

         Deactivates the application belonging to the input project and matching the provided
         application ID.

         Required permissions:
           - project.app.write
      operationId: zitadel.application.v2.ApplicationService.DeactivateApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.DeactivateApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.DeactivateApplicationResponse'
  /zitadel.application.v2.ApplicationService/ReactivateApplication:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: ReactivateApplication
      description: |-
        Reactivate Application

         Reactivates the application belonging to the input project and matching the provided
         application ID.

         Required permissions:
           - project.app.write
      operationId: zitadel.application.v2.ApplicationService.ReactivateApplication
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.ReactivateApplicationRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.ReactivateApplicationResponse'
  /zitadel.application.v2.ApplicationService/GenerateClientSecret:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: GenerateClientSecret
      description: |-
        Generate Client Secret

         Generates the client secret of an API or OIDC application that belongs to the input project.

         Required permissions:
           - project.app.write
      operationId: zitadel.application.v2.ApplicationService.GenerateClientSecret
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.GenerateClientSecretRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.GenerateClientSecretResponse'
  /zitadel.application.v2.ApplicationService/ListApplications:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: ListApplications
      description: |-
        List Applications

         Returns a list of applications matching the input parameters. The results can be filtered
         by project, state, type and name. It can be sorted by id, name, creation date, change date or state.

         Required permissions:
           - project.app.read
      operationId: zitadel.application.v2.ApplicationService.ListApplications
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.ListApplicationsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.ListApplicationsResponse'
  /zitadel.application.v2.ApplicationService/CreateApplicationKey:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: CreateApplicationKey
      description: |-
        Create Application Key

         Create a new application key, which is used to authorize an API application.

         Key details are returned in the response. They must be stored safely, as it will not
         be possible to retrieve them again.

         Required permissions:
           - `project.app.write`
      operationId: zitadel.application.v2.ApplicationService.CreateApplicationKey
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.CreateApplicationKeyRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.CreateApplicationKeyResponse'
  /zitadel.application.v2.ApplicationService/DeleteApplicationKey:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: DeleteApplicationKey
      description: |-
        Delete Application Key

         Deletes an application key matching the provided ID.

         Organization ID is not mandatory, but helps with filtering/performance.

         The deletion time is returned in response message.

         Required permissions:
           - `project.app.write`
      operationId: zitadel.application.v2.ApplicationService.DeleteApplicationKey
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.DeleteApplicationKeyRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.DeleteApplicationKeyResponse'
  /zitadel.application.v2.ApplicationService/GetApplicationKey:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: GetApplicationKey
      description: |-
        Get Application Key

         Retrieves the application key matching the provided ID.

         Specifying a project, organization and application ID is optional but help with filtering/performance.

         Required permissions:
           - project.app.read
      operationId: zitadel.application.v2.ApplicationService.GetApplicationKey
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.GetApplicationKeyRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.GetApplicationKeyResponse'
  /zitadel.application.v2.ApplicationService/ListApplicationKeys:
    post:
      tags:
        - zitadel.application.v2.ApplicationService
      summary: ListApplicationKeys
      description: |-
        List Application Keys

         Returns a list of application keys matching the input parameters.

         The result can be sorted by id, aggregate, creation date, expiration date, resource owner or type.
         It can also be filtered by application, project or organization ID.

         Required permissions:
           - project.app.read
      operationId: zitadel.application.v2.ApplicationService.ListApplicationKeys
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zitadel.application.v2.ListApplicationKeysRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/zitadel.application.v2.ListApplicationKeysResponse'
components:
  schemas:
    zitadel.application.v2.APIAuthMethodType:
      type: string
      title: APIAuthMethodType
      enum:
        - API_AUTH_METHOD_TYPE_BASIC
        - API_AUTH_METHOD_TYPE_PRIVATE_KEY_JWT
    zitadel.application.v2.ApplicationKeysSorting:
      type: string
      title: ApplicationKeysSorting
      enum:
        - APPLICATION_KEYS_SORT_BY_ID
        - APPLICATION_KEYS_SORT_BY_PROJECT_ID
        - APPLICATION_KEYS_SORT_BY_APPLICATION_ID
        - APPLICATION_KEYS_SORT_BY_CREATION_DATE
        - APPLICATION_KEYS_SORT_BY_ORGANIZATION_ID
        - APPLICATION_KEYS_SORT_BY_EXPIRATION
        - APPLICATION_KEYS_SORT_BY_TYPE
    zitadel.application.v2.ApplicationSorting:
      type: string
      title: ApplicationSorting
      enum:
        - APPLICATION_SORT_BY_ID
        - APPLICATION_SORT_BY_NAME
        - APPLICATION_SORT_BY_STATE
        - APPLICATION_SORT_BY_CREATION_DATE
        - APPLICATION_SORT_BY_CHANGE_DATE
    zitadel.application.v2.ApplicationState:
      type: string
      title: ApplicationState
      enum:
        - APPLICATION_STATE_UNSPECIFIED
        - APPLICATION_STATE_ACTIVE
        - APPLICATION_STATE_INACTIVE
        - APPLICATION_STATE_REMOVED
    zitadel.application.v2.ApplicationType:
      type: string
      title: ApplicationType
      enum:
        - APPLICATION_TYPE_UNSPECIFIED
        - APPLICATION_TYPE_OIDC
        - APPLICATION_TYPE_API
        - APPLICATION_TYPE_SAML
    zitadel.application.v2.OIDCApplicationType:
      type: string
      title: OIDCApplicationType
      enum:
        - OIDC_APP_TYPE_WEB
        - OIDC_APP_TYPE_USER_AGENT
        - OIDC_APP_TYPE_NATIVE
    zitadel.application.v2.OIDCAuthMethodType:
      type: string
      title: OIDCAuthMethodType
      enum:
        - OIDC_AUTH_METHOD_TYPE_BASIC
        - OIDC_AUTH_METHOD_TYPE_POST
        - OIDC_AUTH_METHOD_TYPE_NONE
        - OIDC_AUTH_METHOD_TYPE_PRIVATE_KEY_JWT
    zitadel.application.v2.OIDCGrantType:
      type: string
      title: OIDCGrantType
      enum:
        - OIDC_GRANT_TYPE_AUTHORIZATION_CODE
        - OIDC_GRANT_TYPE_IMPLICIT
        - OIDC_GRANT_TYPE_REFRESH_TOKEN
        - OIDC_GRANT_TYPE_DEVICE_CODE
        - OIDC_GRANT_TYPE_TOKEN_EXCHANGE
    zitadel.application.v2.OIDCResponseType:
      type: string
      title: OIDCResponseType
      enum:
        - OIDC_RESPONSE_TYPE_UNSPECIFIED
        - OIDC_RESPONSE_TYPE_CODE
        - OIDC_RESPONSE_TYPE_ID_TOKEN
        - OIDC_RESPONSE_TYPE_ID_TOKEN_TOKEN
    zitadel.application.v2.OIDCTokenType:
      type: string
      title: OIDCTokenType
      enum:
        - OIDC_TOKEN_TYPE_BEARER
        - OIDC_TOKEN_TYPE_JWT
    zitadel.application.v2.OIDCVersion:
      type: string
      title: OIDCVersion
      enum:
        - OIDC_VERSION_1_0
    zitadel.filter.v2.TextFilterMethod:
      type: string
      title: TextFilterMethod
      enum:
        - TEXT_FILTER_METHOD_EQUALS
        - TEXT_FILTER_METHOD_EQUALS_IGNORE_CASE
        - TEXT_FILTER_METHOD_STARTS_WITH
        - TEXT_FILTER_METHOD_STARTS_WITH_IGNORE_CASE
        - TEXT_FILTER_METHOD_CONTAINS
        - TEXT_FILTER_METHOD_CONTAINS_IGNORE_CASE
        - TEXT_FILTER_METHOD_ENDS_WITH
        - TEXT_FILTER_METHOD_ENDS_WITH_IGNORE_CASE
    google.protobuf.Duration:
      type: string
      format: duration
      description: |-
        A Duration represents a signed, fixed-length span of time represented
         as a count of seconds and fractions of seconds at nanosecond
         resolution. It is independent of any calendar and concepts like "day"
         or "month". It is related to Timestamp in that the difference between
         two Timestamp values is a Duration and it can be added or subtracted
         from a Timestamp. Range is approximately +-10,000 years.

         # Examples

         Example 1: Compute Duration from two Timestamps in pseudo code.

             Timestamp start = ...;
             Timestamp end = ...;
             Duration duration = ...;

             duration.seconds = end.seconds - start.seconds;
             duration.nanos = end.nanos - start.nanos;

             if (duration.seconds < 0 && duration.nanos > 0) {
               duration.seconds += 1;
               duration.nanos -= 1000000000;
             } else if (duration.seconds > 0 && duration.nanos < 0) {
               duration.seconds -= 1;
               duration.nanos += 1000000000;
             }

         Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.

             Timestamp start = ...;
             Duration duration = ...;
             Timestamp end = ...;

             end.seconds = start.seconds + duration.seconds;
             end.nanos = start.nanos + duration.nanos;

             if (end.nanos < 0) {
               end.seconds -= 1;
               end.nanos += 1000000000;
             } else if (end.nanos >= 1000000000) {
               end.seconds += 1;
               end.nanos -= 1000000000;
             }

         Example 3: Compute Duration from datetime.timedelta in Python.

             td = datetime.timedelta(days=3, minutes=10)
             duration = Duration()
             duration.FromTimedelta(td)

         # JSON Mapping

         In JSON format, the Duration type is encoded as a string rather than an
         object, where the string ends in the suffix "s" (indicating seconds) and
         is preceded by the number of seconds, with nanoseconds expressed as
         fractional seconds. For example, 3 seconds with 0 nanoseconds should be
         encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
         be expressed in JSON format as "3.000000001s", and 3 seconds and 1
         microsecond should be expressed in JSON format as "3.000001s".
    google.protobuf.Timestamp:
      type: string
      examples:
        - 1s
        - 1.000340012s
      format: date-time
      description: |-
        A Timestamp represents a point in time independent of any time zone or local
         calendar, encoded as a count of seconds and fractions of seconds at
         nanosecond resolution. The count is relative to an epoch at UTC midnight on
         January 1, 1970, in the proleptic Gregorian calendar which extends the
         Gregorian calendar backwards to year one.

         All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
         second table is needed for interpretation, using a [24-hour linear
         smear](https://developers.google.com/time/smear).

         The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
         restricting to that range, we ensure that we can convert to and from [RFC
         3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

         # Examples

         Example 1: Compute Timestamp from POSIX `time()`.

             Timestamp timestamp;
             timestamp.set_seconds(time(NULL));
             timestamp.set_nanos(0);

         Example 2: Compute Timestamp from POSIX `gettimeofday()`.

             struct timeval tv;
             gettimeofday(&tv, NULL);

             Timestamp timestamp;
             timestamp.set_seconds(tv.tv_sec);
             timestamp.set_nanos(tv.tv_usec * 1000);

         Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

             FILETIME ft;
             GetSystemTimeAsFileTime(&ft);
             UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

             // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
             // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
             Timestamp timestamp;
             timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
             timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

         Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

             long millis = System.currentTimeMillis();

             Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
                 .setNanos((int) ((millis % 1000) * 1000000)).build();

         Example 5: Compute Timestamp from Java `Instant.now()`.

             Instant now = Instant.now();

             Timestamp timestamp =
                 Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                     .setNanos(now.getNano()).build();

         Example 6: Compute Timestamp from current time in Python.

             timestamp = Timestamp()
             timestamp.GetCurrentTime()

         # JSON Mapping

         In JSON format, the Timestamp type is encoded as a string in the
         [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
         format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
         where {year} is always expressed using four digits while {month}, {day},
         {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
         seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
         are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
         is required. A proto3 JSON serializer should always use UTC (as indicated by
         "Z") when printing the Timestamp type and a proto3 JSON parser should be
         able to accept both UTC and other timezones (as indicated by an offset).

         For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
         01:30 UTC on January 15, 2017.

         In JavaScript, one can convert a Date object to this format using the
         standard
         [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
         method. In Python, a standard `datetime.datetime` object can be converted
         to this format using
         [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
         the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
         the Joda Time's [`ISODateTimeFormat.dateTime()`](
         http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
         ) to obtain a formatter capable of generating timestamps in this format.
    zitadel.application.v2.APIConfiguration:
      type: object
      properties:
        clientId:
          type: string
          title: client_id
          description: |-
            The unique OAuth2 client_id used for authentication of the API,
             e.g. at the introspection endpoint.
        authMethodType:
          title: auth_method_type
          description: The authentication method type used by the API to authenticate at the introspection endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.APIAuthMethodType'
      title: APIConfiguration
      additionalProperties: false
    zitadel.application.v2.Application:
      type: object
      oneOf:
        - properties:
            apiConfiguration:
              title: api_configuration
              $ref: '#/components/schemas/zitadel.application.v2.APIConfiguration'
          title: api_configuration
          required:
            - apiConfiguration
        - properties:
            oidcConfiguration:
              title: oidc_configuration
              $ref: '#/components/schemas/zitadel.application.v2.OIDCConfiguration'
          title: oidc_configuration
          required:
            - oidcConfiguration
        - properties:
            samlConfiguration:
              title: saml_configuration
              $ref: '#/components/schemas/zitadel.application.v2.SAMLConfiguration'
          title: saml_configuration
          required:
            - samlConfiguration
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique identifier of the application.
        creationDate:
          title: creation_date
          description: The timestamp of the application creation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        changeDate:
          title: change_date
          description: The timestamp of the last update to the application.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        state:
          title: state
          description: The current state of the application.
          $ref: '#/components/schemas/zitadel.application.v2.ApplicationState'
        name:
          type: string
          title: name
          description: The name of the application. This can be displayed to users.
      title: Application
      additionalProperties: false
    zitadel.application.v2.ApplicationKey:
      type: object
      properties:
        keyId:
          type: string
          title: key_id
          description: The unique identifier of the application key.
        applicationId:
          type: string
          title: application_id
          description: The identifier of the application this key belongs to.
        projectId:
          type: string
          title: project_id
          description: The identifier of the project this application belongs to.
        creationDate:
          title: creation_date
          description: The timestamp of the key creation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        organizationId:
          type: string
          title: organization_id
          description: The identifier of the organization this application belongs to.
        expirationDate:
          title: expiration_date
          description: The timestamp the key expires.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: ApplicationKey
      additionalProperties: false
    zitadel.application.v2.ApplicationKeyApplicationIDFilter:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: Search for application keys belonging to the application with this ID.
      title: ApplicationKeyApplicationIDFilter
      required:
        - applicationId
      additionalProperties: false
    zitadel.application.v2.ApplicationKeyOrganizationIDFilter:
      type: object
      properties:
        organizationId:
          type: string
          title: organization_id
          description: Search for application keys belonging to applications in the organization with this ID.
      title: ApplicationKeyOrganizationIDFilter
      required:
        - organizationId
      additionalProperties: false
    zitadel.application.v2.ApplicationKeyProjectIDFilter:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
          description: Search for application keys belonging to applications in the project with this ID.
      title: ApplicationKeyProjectIDFilter
      required:
        - projectId
      additionalProperties: false
    zitadel.application.v2.ApplicationKeySearchFilter:
      type: object
      oneOf:
        - properties:
            applicationIdFilter:
              title: application_id_filter
              description: Filter the application keys by the application ID they belong to.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationKeyApplicationIDFilter'
          title: application_id_filter
          required:
            - applicationIdFilter
        - properties:
            organizationIdFilter:
              title: organization_id_filter
              description: Filter the application keys by the organization ID the corresponding application belong to.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationKeyOrganizationIDFilter'
          title: organization_id_filter
          required:
            - organizationIdFilter
        - properties:
            projectIdFilter:
              title: project_id_filter
              description: Filter the application keys by the project ID the corresponding application belong to.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationKeyProjectIDFilter'
          title: project_id_filter
          required:
            - projectIdFilter
      title: ApplicationKeySearchFilter
      additionalProperties: false
    zitadel.application.v2.ApplicationNameFilter:
      type: object
      properties:
        name:
          type: string
          title: name
          description: The name of the application to search for.
        method:
          title: method
          description: The method to use for text comparison. If not set, defaults to EQUALS.
          $ref: '#/components/schemas/zitadel.filter.v2.TextFilterMethod'
      title: ApplicationNameFilter
      additionalProperties: false
    zitadel.application.v2.ApplicationSearchFilter:
      type: object
      oneOf:
        - properties:
            nameFilter:
              title: name_filter
              description: Filter the applications by their name.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationNameFilter'
          title: name_filter
          required:
            - nameFilter
        - properties:
            projectIdFilter:
              title: project_id_filter
              description: Filter the projectID the application has to belong to.
              $ref: '#/components/schemas/zitadel.application.v2.ProjectIDFilter'
          title: project_id_filter
          required:
            - projectIdFilter
        - properties:
            stateFilter:
              title: state_filter
              description: Filter the applications by their state.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationState'
          title: state_filter
          required:
            - stateFilter
        - properties:
            typeFilter:
              title: type_filter
              description: Filter for specific application types.
              $ref: '#/components/schemas/zitadel.application.v2.ApplicationType'
          title: type_filter
          required:
            - typeFilter
      title: ApplicationSearchFilter
      additionalProperties: false
    zitadel.application.v2.CreateAPIApplicationRequest:
      type: object
      properties:
        authMethodType:
          title: auth_method_type
          description: The authentication method type used by the API to authenticate at the introspection endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.APIAuthMethodType'
      title: CreateAPIApplicationRequest
      additionalProperties: false
    zitadel.application.v2.CreateAPIApplicationResponse:
      type: object
      properties:
        clientId:
          type: string
          title: client_id
          description: The unique OAuth2 client_id used for authentication of the API, e.g. at the introspection endpoint.
        clientSecret:
          type: string
          title: client_secret
          description: |-
            In case of using the APIAuthMethodType.API_AUTH_METHOD_TYPE_BASIC the client_secret is generated and returned.
             It must be stored safely, as it will not be possible to retrieve it again.
             A new client_secret can be generated using the GenerateClientSecret endpoint.
      title: CreateAPIApplicationResponse
      additionalProperties: false
    zitadel.application.v2.CreateApplicationKeyRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The ID of the application the key will be created for.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
        expirationDate:
          title: expiration_date
          description: The timestamp the key will expire.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: CreateApplicationKeyRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.CreateApplicationKeyResponse:
      type: object
      properties:
        keyId:
          type: string
          title: key_id
          description: The unique ID of the newly created application key.
        creationDate:
          title: creation_date
          description: The timestamp of the application key creation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        keyDetails:
          type: string
          title: key_details
          format: byte
          description: |-
            KeyDetails contains the serialized private key and additional information depending on the key type.
             It must be stored safely, as it will not be possible to retrieve it again.
             A new key can be generated using the CreateApplicationKey endpoint.
      title: CreateApplicationKeyResponse
      additionalProperties: false
    zitadel.application.v2.CreateApplicationRequest:
      type: object
      oneOf:
        - properties:
            apiConfiguration:
              title: api_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateAPIApplicationRequest'
          title: api_configuration
          required:
            - apiConfiguration
        - properties:
            oidcConfiguration:
              title: oidc_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateOIDCApplicationRequest'
          title: oidc_configuration
          required:
            - oidcConfiguration
        - properties:
            samlConfiguration:
              title: saml_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateSAMLApplicationRequest'
          title: saml_configuration
          required:
            - samlConfiguration
      properties:
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application will be created in.
        applicationId:
          type: string
          title: application_id
          description: |-
            Optionally, provide the unique ID of the new application. If omitted, the system will generate one for you,
             which is the recommended way. The generated ID will be returned in the response.
        name:
          type: string
          title: name
          description: Publicly visible name of the application. This might be presented to users if they sign in.
      title: CreateApplicationRequest
      required:
        - projectId
        - name
      additionalProperties: false
    zitadel.application.v2.CreateApplicationResponse:
      type: object
      oneOf:
        - properties:
            apiConfiguration:
              title: api_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateAPIApplicationResponse'
          title: api_configuration
          required:
            - apiConfiguration
        - properties:
            oidcConfiguration:
              title: oidc_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateOIDCApplicationResponse'
          title: oidc_configuration
          required:
            - oidcConfiguration
        - properties:
            samlConfiguration:
              title: saml_configuration
              $ref: '#/components/schemas/zitadel.application.v2.CreateSAMLApplicationResponse'
          title: saml_configuration
          required:
            - samlConfiguration
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the newly created application.
        creationDate:
          title: creation_date
          description: The timestamp of the application creation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: CreateApplicationResponse
      additionalProperties: false
    zitadel.application.v2.CreateOIDCApplicationRequest:
      type: object
      properties:
        redirectUris:
          type: array
          items:
            type: string
          title: redirect_uris
          description: |-
            RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows,
             where the authorization code or tokens will be sent to.
             The redirect_uri parameter in the authorization request must exactly match one of these URIs.
        responseTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCResponseType'
          title: response_types
          description: |-
            ResponseTypes define whether a code, id_token token or just id_token will be returned.
             The response_type parameter in the authorization request must exactly match one of these values.
        grantTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCGrantType'
          title: grant_types
          description: |-
            GrantTypes define the flow type the application is allowed to use.
             The grant_type parameter in the token request must exactly match one of these values.
             Minimum one grant type must be provided, but multiple grant types can be provided to allow
             different flows, e.g. authorization code flow and refresh token flow.
        applicationType:
          title: application_type
          description: |-
            ApplicationType defines the OAuth2/OIDC client type and their ability to maintain
             confidentiality of their credentials.
             This influences the allowed grant types and the required authentication method.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCApplicationType'
        authMethodType:
          title: auth_method_type
          description: The authentication method type used by the application to authenticate at the token endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCAuthMethodType'
        postLogoutRedirectUris:
          type: array
          items:
            type: string
          title: post_logout_redirect_uris
          description: |-
            PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout.
             The post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.
        version:
          title: version
          description: |-
            Version defines the OIDC version used by the application.
             Currently, only version 1.0 is supported.
             Future versions might introduce breaking changes.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCVersion'
        developmentMode:
          type: boolean
          title: development_mode
          description: |-
            DevelopmentMode can be enabled for development purposes. This allows the use of
             OIDC non-compliant and potentially insecure settings, such as the use of
             HTTP redirect URIs or wildcard redirect URIs.
        accessTokenType:
          title: access_token_type
          description: |-
            The AccessTokenType defines the type of the access token returned from ZITADEL.
             Bearer tokens are opaque to clients. JWT tokens are self-contained and can be validated by the client.
             Bearer tokens must be introspected at the ZITADEL token endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCTokenType'
        accessTokenRoleAssertion:
          type: boolean
          title: access_token_role_assertion
          description: |-
            If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.
             Ensure that the access token is a JWT token and not a bearer token. And either request the roles
             by scope or enable the user role assertion on the project.
        idTokenRoleAssertion:
          type: boolean
          title: id_token_role_assertion
          description: |-
            If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token.
             Ensure that either the roles are requested by scope or enable the user role assertion on the
             project.
        idTokenUserinfoAssertion:
          type: boolean
          title: id_token_userinfo_assertion
          description: |-
            If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes
             are added to the id token even if an access token is issued. This can be required by some applications
             that do not call the userinfo endpoint after authentication or directly use the id_token for retrieving
             user information.
             Attention: this violates the OIDC specification, which states that these claims must only be
             requested from the userinfo endpoint if an access token is issued. This is to prevent
             leaking of personal information in the id token, which is often stored in the browser and
             therefore more vulnerable.
        clockSkew:
          title: clock_skew
          description: |-
            ClockSkew is used to compensate time differences between the servers of ZITADEL and the application.
             It is added to the "exp" claim and subtracted from "iat", "auth_time" and "nbf" claims.
             The default is 0s, the maximum is 5s.
          $ref: '#/components/schemas/google.protobuf.Duration'
        additionalOrigins:
          type: array
          items:
            type: string
          title: additional_origins
          description: |-
            AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used
             additional to the redirect_uris.
             This is useful if the application is used from an origin different to the redirect_uris,
             e.g. if the application is a SPA served in a native app, where the redirect_uri is a custom scheme,
             but the application is served from a https origin.
        skipNativeAppSuccessPage:
          type: boolean
          title: skip_native_app_success_page
          description: |-
            For native apps a successful login usually shows a success page with a link to open the application again.
             SkipNativeAppSuccessPage can be used to skip this page and open the application directly.
        backChannelLogoutUri:
          type: string
          title: back_channel_logout_uri
          description: |-
            BackChannelLogoutURI is used to notify the application about terminated sessions according
             to the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: CreateOIDCApplicationRequest
      additionalProperties: false
    zitadel.application.v2.CreateOIDCApplicationResponse:
      type: object
      properties:
        clientId:
          type: string
          title: client_id
          description: |-
            The unique OAuth2/OIDC client_id used for authentication of the application,
             e.g. at the token endpoint.
        clientSecret:
          type: string
          title: client_secret
          description: |-
            In case of using the OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_BASIC
             or OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_POST the client_secret is generated and returned.
             It must be stored safely, as it will not be possible to retrieve it again.
             A new client_secret can be generated using the GenerateClientSecret endpoint.
        nonCompliant:
          type: boolean
          title: non_compliant
          description: |-
            NonCompliant specifies whether the config is OIDC compliant. A production configuration SHOULD be compliant.
             Non-compliant configurations can run into interoperability issues with OIDC libraries and tools.
             Compliance problems are listed in the compliance_problems field.
        complianceProblems:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCLocalizedMessage'
          title: compliance_problems
          description: |-
            ComplianceProblems lists the problems for non-compliant configurations.
             In case of a compliant configuration, this list is empty.
      title: CreateOIDCApplicationResponse
      additionalProperties: false
    zitadel.application.v2.CreateSAMLApplicationRequest:
      type: object
      oneOf:
        - properties:
            metadataUrl:
              type: string
              title: metadata_url
          title: metadata_url
          required:
            - metadataUrl
        - properties:
            metadataXml:
              type: string
              title: metadata_xml
              format: byte
          title: metadata_xml
          required:
            - metadataXml
      properties:
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: CreateSAMLApplicationRequest
      additionalProperties: false
    zitadel.application.v2.CreateSAMLApplicationResponse:
      type: object
      title: CreateSAMLApplicationResponse
      additionalProperties: false
    zitadel.application.v2.DeactivateApplicationRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to be deactivated.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
      title: DeactivateApplicationRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.DeactivateApplicationResponse:
      type: object
      properties:
        deactivationDate:
          title: deactivation_date
          description: The timestamp of the application deactivation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: DeactivateApplicationResponse
      additionalProperties: false
    zitadel.application.v2.DeleteApplicationKeyRequest:
      type: object
      properties:
        keyId:
          type: string
          title: key_id
          description: The unique ID of the application key to be deleted.
        applicationId:
          type: string
          title: application_id
          description: The ID of the application the key belongs to.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
      title: DeleteApplicationKeyRequest
      required:
        - keyId
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.DeleteApplicationKeyResponse:
      type: object
      properties:
        deletionDate:
          title: deletion_date
          description: |-
            The timestamp of the application key deletion. In case the key was already deleted,
             the previous deletion date is returned. This can be used to check if the key was
             actually deleted.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: DeleteApplicationKeyResponse
      additionalProperties: false
    zitadel.application.v2.DeleteApplicationRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to be deleted.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
      title: DeleteApplicationRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.DeleteApplicationResponse:
      type: object
      properties:
        deletionDate:
          title: deletion_date
          description: |-
            The timestamp of the application deletion. In case the application was already deleted,
             the previous deletion date is returned. This can be used to check if the application was
             actually deleted.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: DeleteApplicationResponse
      additionalProperties: false
    zitadel.application.v2.GenerateClientSecretRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to generate a new client secret for.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
      title: GenerateClientSecretRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.GenerateClientSecretResponse:
      type: object
      properties:
        clientSecret:
          type: string
          title: client_secret
          description: |-
            The newly generated client secret. It must be stored safely, as it will not be possible to retrieve it again.
             A new client secret can be generated using this endpoint.
        creationDate:
          title: creation_date
          description: The timestamp of the creation of the new client secret.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: GenerateClientSecretResponse
      additionalProperties: false
    zitadel.application.v2.GetApplicationKeyRequest:
      type: object
      properties:
        keyId:
          type: string
          title: key_id
          description: The unique ID of the application key to be retrieved.
      title: GetApplicationKeyRequest
      required:
        - keyId
      additionalProperties: false
    zitadel.application.v2.GetApplicationKeyResponse:
      type: object
      properties:
        keyId:
          type: string
          title: key_id
          description: Unique ID of the application key.
        creationDate:
          title: creation_date
          description: The timestamp of the key creation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        expirationDate:
          title: expiration_date
          description: The timestamp when the key will expire.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: GetApplicationKeyResponse
      additionalProperties: false
    zitadel.application.v2.GetApplicationRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to be retrieved.
      title: GetApplicationRequest
      required:
        - applicationId
      additionalProperties: false
    zitadel.application.v2.GetApplicationResponse:
      type: object
      properties:
        application:
          title: application
          $ref: '#/components/schemas/zitadel.application.v2.Application'
      title: GetApplicationResponse
      additionalProperties: false
    zitadel.application.v2.ListApplicationKeysRequest:
      type: object
      properties:
        pagination:
          title: pagination
          description: Pagination and sorting.
          $ref: '#/components/schemas/zitadel.filter.v2.PaginationRequest'
        sortingColumn:
          title: sorting_column
          description: The column to sort by. If not provided, the default is 'KEY_ID'.
          $ref: '#/components/schemas/zitadel.application.v2.ApplicationKeysSorting'
        filters:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.ApplicationKeySearchFilter'
          title: filters
          description: |-
            Criteria to filter the application keys.
             All provided filters are combined with a logical AND.
      title: ListApplicationKeysRequest
      additionalProperties: false
    zitadel.application.v2.ListApplicationKeysResponse:
      type: object
      properties:
        keys:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.ApplicationKey'
          title: keys
          description: |-
            The list of application keys matching the query. Depending on the applied limit,
             there might be more keys available than returned in this list.
             Use the returned pagination information to request further keys.
        pagination:
          title: pagination
          description: Contains the total number of application keys matching the query and the applied limit.
          $ref: '#/components/schemas/zitadel.filter.v2.PaginationResponse'
      title: ListApplicationKeysResponse
      additionalProperties: false
    zitadel.application.v2.ListApplicationsRequest:
      type: object
      properties:
        pagination:
          title: pagination
          description: Pagination and sorting.
          $ref: '#/components/schemas/zitadel.filter.v2.PaginationRequest'
        sortingColumn:
          title: sorting_column
          $ref: '#/components/schemas/zitadel.application.v2.ApplicationSorting'
        filters:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.ApplicationSearchFilter'
          title: filters
          description: |-
            Criteria to filter the applications.
             All provided filters are combined with a logical AND.
      title: ListApplicationsRequest
      additionalProperties: false
    zitadel.application.v2.ListApplicationsResponse:
      type: object
      properties:
        applications:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.Application'
          title: applications
          description: |-
            The list of applications matching the query. Depending on the applied limit,
             there might be more applications available than included in this list.
             Use the returned pagination information to request further applications.
        pagination:
          title: pagination
          description: Contains the total number of apps matching the query and the applied limit.
          $ref: '#/components/schemas/zitadel.filter.v2.PaginationResponse'
      title: ListApplicationsResponse
      additionalProperties: false
    zitadel.application.v2.LoginV1:
      type: object
      title: LoginV1
      additionalProperties: false
    zitadel.application.v2.LoginV2:
      type: object
      properties:
        baseUri:
          type: string
          title: base_uri
          description: Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
          nullable: true
      title: LoginV2
      additionalProperties: false
    zitadel.application.v2.LoginVersion:
      type: object
      oneOf:
        - properties:
            loginV1:
              title: login_v1
              description: Allow the user to sign in through the ZITADEL hosted login UI.
              $ref: '#/components/schemas/zitadel.application.v2.LoginV1'
          title: login_v1
          required:
            - loginV1
        - properties:
            loginV2:
              title: login_v2
              description: Allow the user to sign in though the new login UI or even your own UI by specifying a base_uri.
              $ref: '#/components/schemas/zitadel.application.v2.LoginV2'
          title: login_v2
          required:
            - loginV2
      title: LoginVersion
      additionalProperties: false
    zitadel.application.v2.OIDCConfiguration:
      type: object
      properties:
        redirectUris:
          type: array
          items:
            type: string
          title: redirect_uris
          description: |-
            RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows,
             where the authorization code or tokens will be sent to.
             The redirect_uri parameter in the authorization request must exactly match one of these URIs.
        responseTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCResponseType'
          title: response_types
          description: |-
            ResponseTypes define whether a code, id_token token or just id_token will be returned.
             The response_type parameter in the authorization request must exactly match one of these values.
        grantTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCGrantType'
          title: grant_types
          description: |-
            GrantTypes define the flow type the application is allowed to use.
             The grant_type parameter in the token request must exactly match one of these values.
        applicationType:
          title: application_type
          description: |-
            ApplicationType defines the OAuth2/OIDC client type and their ability to maintain
             confidentiality of their credentials.
             This influences the allowed grant types and the required authentication method.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCApplicationType'
        clientId:
          type: string
          title: client_id
          description: |-
            The unique OAuth2/OIDC client_id used for authentication of the application,
             e.g. at the token endpoint.
        authMethodType:
          title: auth_method_type
          description: The authentication method type used by the application to authenticate at the token endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCAuthMethodType'
        postLogoutRedirectUris:
          type: array
          items:
            type: string
          title: post_logout_redirect_uris
          description: |-
            PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout.
             The post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.
        version:
          title: version
          description: |-
            Version defines the OIDC version used by the application.
             Currently, only version 1.0 is supported.
             Future versions might introduce breaking changes.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCVersion'
        nonCompliant:
          type: boolean
          title: non_compliant
          description: |-
            NonCompliant specifies whether the config is OIDC compliant. A production configuration SHOULD be compliant.
             Non-compliant configurations can run into interoperability issues with OIDC libraries and tools.
             Compliance problems are listed in the compliance_problems field.
        complianceProblems:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCLocalizedMessage'
          title: compliance_problems
          description: |-
            ComplianceProblems lists the problems for non-compliant configurations.
             In case of a compliant configuration, this list is empty.
        developmentMode:
          type: boolean
          title: development_mode
          description: |-
            DevelopmentMode can be enabled for development purposes. This allows the use of
             OIDC non-compliant and potentially insecure settings, such as the use of
             HTTP redirect URIs or wildcard redirect URIs.
        accessTokenType:
          title: access_token_type
          description: |-
            The AccessTokenType defines the type of the access token returned from ZITADEL.
             Bearer tokens are opaque to clients. JWT tokens are self-contained and can be validated by the client.
             Bearer tokens must be introspected at the ZITADEL token endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.OIDCTokenType'
        accessTokenRoleAssertion:
          type: boolean
          title: access_token_role_assertion
          description: |-
            If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.
             Ensure that the access token is a JWT token and not a bearer token. And either request the roles
             by scope or enable the user role assertion on the project.
        idTokenRoleAssertion:
          type: boolean
          title: id_token_role_assertion
          description: |-
            If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token.
             Ensure that either the roles are requested by scope or enable the user role assertion on the
             project.
        idTokenUserinfoAssertion:
          type: boolean
          title: id_token_userinfo_assertion
          description: |-
            If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes
             are added to the id token even if an access token is issued. This can be required by some applications
             that do not call the userinfo endpoint after authentication or directly use the id_token for retrieving
             user information.
             Attention: this violates the OIDC specification, which states that these claims must only be
             requested from the userinfo endpoint if an access token is issued. This is to prevent
             leaking of personal information in the id token, which is often stored in the browser and
             therefore more vulnerable.
        clockSkew:
          title: clock_skew
          description: |-
            ClockSkew is used to compensate time differences between the servers of ZITADEL and the application.
             It is added to the "exp" claim and subtracted from "iat", "auth_time" and "nbf" claims.
             The default is 0s, the maximum is 5s.
          $ref: '#/components/schemas/google.protobuf.Duration'
        additionalOrigins:
          type: array
          items:
            type: string
          title: additional_origins
          description: |-
            AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used
             additional to the redirect_uris.
             This is useful if the application is used from an origin different to the redirect_uris,
             e.g. if the application is a SPA served in a native app, where the redirect_uri is a custom scheme,
             but the application is served from a https origin.
        allowedOrigins:
          type: array
          items:
            type: string
          title: allowed_origins
          description: |-
            AllowedOrigins are all HTTP origins where the application is allowed to be used from.
             This is used to prevent CORS issues in browsers.
             If the origin of the request is not in this list, the request will be rejected.
             This is especially important for SPAs.
             Note that this is a generated list from the redirect_uris and additional_origins.
             If you use the application from another origin, you have to add it to the additional_origins.
        skipNativeAppSuccessPage:
          type: boolean
          title: skip_native_app_success_page
          description: |-
            For native apps a successful login usually shows a success page with a link to open the application again.
             SkipNativeAppSuccessPage can be used to skip this page and open the application directly.
        backChannelLogoutUri:
          type: string
          title: back_channel_logout_uri
          description: |-
            BackChannelLogoutURI is used to notify the application about terminated sessions according
             to the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: OIDCConfiguration
      additionalProperties: false
    zitadel.application.v2.OIDCLocalizedMessage:
      type: object
      properties:
        key:
          type: string
          title: key
        localizedMessage:
          type: string
          title: localized_message
      title: OIDCLocalizedMessage
      additionalProperties: false
    zitadel.application.v2.ProjectIDFilter:
      type: object
      properties:
        projectId:
          type: string
          title: project_id
          description: Search for application belonging to the project with this ID.
      title: ProjectIDFilter
      required:
        - projectId
      additionalProperties: false
    zitadel.application.v2.ReactivateApplicationRequest:
      type: object
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to be reactivated.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
      title: ReactivateApplicationRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.ReactivateApplicationResponse:
      type: object
      properties:
        reactivationDate:
          title: reactivation_date
          description: The timestamp of the application reactivation.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: ReactivateApplicationResponse
      additionalProperties: false
    zitadel.application.v2.SAMLConfiguration:
      type: object
      properties:
        metadataXml:
          type: string
          title: metadata_xml
          format: byte
          description: |-
            The Metadata XML is the provided or fetched metadata stored at Zitadel.
             If either the metadata was provided as XML or when Zitadel fetched it at the provided URL,
             it is stored here.
        metadataUrl:
          type: string
          title: metadata_url
          description: |-
            The Metadata URL is the URL where the metadata was fetched from.
             In case the metadata was provided as raw XML, this field is empty.
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: SAMLConfiguration
      additionalProperties: false
    zitadel.application.v2.UpdateAPIApplicationConfigurationRequest:
      type: object
      properties:
        authMethodType:
          title: auth_method_type
          description: The authentication method type used by the API to authenticate at the introspection endpoint.
          $ref: '#/components/schemas/zitadel.application.v2.APIAuthMethodType'
      title: UpdateAPIApplicationConfigurationRequest
      additionalProperties: false
    zitadel.application.v2.UpdateApplicationRequest:
      type: object
      oneOf:
        - properties:
            apiConfiguration:
              title: api_configuration
              $ref: '#/components/schemas/zitadel.application.v2.UpdateAPIApplicationConfigurationRequest'
          title: api_configuration
          required:
            - apiConfiguration
        - properties:
            oidcConfiguration:
              title: oidc_configuration
              $ref: '#/components/schemas/zitadel.application.v2.UpdateOIDCApplicationConfigurationRequest'
          title: oidc_configuration
          required:
            - oidcConfiguration
        - properties:
            samlConfiguration:
              title: saml_configuration
              $ref: '#/components/schemas/zitadel.application.v2.UpdateSAMLApplicationConfigurationRequest'
          title: saml_configuration
          required:
            - samlConfiguration
      properties:
        applicationId:
          type: string
          title: application_id
          description: The unique ID of the application to be updated.
        projectId:
          type: string
          title: project_id
          description: The ID of the project the application belongs to.
        name:
          type: string
          title: name
          description: |-
            Publicly visible name of the application. This might be presented to users if they sign in.
             If not set, the name will not be changed.
      title: UpdateApplicationRequest
      required:
        - applicationId
        - projectId
      additionalProperties: false
    zitadel.application.v2.UpdateApplicationResponse:
      type: object
      properties:
        changeDate:
          title: change_date
          description: |-
            The timestamp of the application update. If no changes were made, the previous change date is returned.
             This can be used to check if the application was actually updated.
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: UpdateApplicationResponse
      additionalProperties: false
    zitadel.application.v2.UpdateOIDCApplicationConfigurationRequest:
      type: object
      properties:
        redirectUris:
          type: array
          items:
            type: string
          title: redirect_uris
          description: |-
            RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows,
             where the authorization code or tokens will be sent to.
             The redirect_uri parameter in the authorization request must exactly match one of these URIs.
             Any existing redirect URIs not included in this list will be removed.
             If not set, the redirect URIs will not be changed.
        responseTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCResponseType'
          title: response_types
          description: |-
            ResponseTypes define whether a code, id_token token or just id_token will be returned.
             The response_type parameter in the authorization request must exactly match one of these values.
             Any existing response types not included in this list will be removed.
             If not set, the response types will not be changed.
        grantTypes:
          type: array
          items:
            $ref: '#/components/schemas/zitadel.application.v2.OIDCGrantType'
          title: grant_types
          description: |-
            GrantTypes define the flow type the application is allowed to use.
             The grant_type parameter in the token request must exactly match one of these values.
             Minimum one grant type must be provided, but multiple grant types can be provided to allow
             different flows, e.g. authorization code flow and refresh token flow.
             Any existing grant types not included in this list will be removed.
             If not set, the grant types will not be changed.
        applicationType:
          title: application_type
          description: |-
            ApplicationType defines the OAuth2/OIDC client type and their ability to maintain
             confidentiality of their credentials.
             This influences the allowed grant types and the required authentication method.
             If not set, the application type will not be changed.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.OIDCApplicationType'
        authMethodType:
          title: auth_method_type
          description: |-
            The authentication method type used by the application to authenticate at the token endpoint.
             If not set, the authentication method type will not be changed.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.OIDCAuthMethodType'
        postLogoutRedirectUris:
          type: array
          items:
            type: string
          title: post_logout_redirect_uris
          description: |-
            PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout.
             The post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.
             Any existing post logout redirect URIs not included in this list will be removed.
             If not set, the post logout redirect URIs will not be changed.
        version:
          title: version
          description: |-
            Version defines the OIDC version used by the application.
             Currently, only version 1.0 is supported.
             Future versions might introduce breaking changes.
             If not set, the version will not be changed.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.OIDCVersion'
        developmentMode:
          type: boolean
          title: development_mode
          description: |-
            DevelopmentMode can be enabled for development purposes. This allows the use of
             OIDC non-compliant and potentially insecure settings, such as the use of
             HTTP redirect URIs or wildcard redirect URIs.
             If not set, the dev mode will not be changed.
          nullable: true
        accessTokenType:
          title: access_token_type
          description: |-
            The AccessTokenType defines the type of the access token returned from ZITADEL.
             Bearer tokens are opaque to clients. JWT tokens are self-contained and can be validated by the client.
             Bearer tokens must be introspected at the ZITADEL token endpoint.
             If not set, the access token type will not be changed.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.OIDCTokenType'
        accessTokenRoleAssertion:
          type: boolean
          title: access_token_role_assertion
          description: |-
            If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.
             Ensure that the access token is a JWT token and not a bearer token. And either request the roles
             by scope or enable the user role assertion on the project.
             If not set, the access token role assertion will not be changed.
          nullable: true
        idTokenRoleAssertion:
          type: boolean
          title: id_token_role_assertion
          description: |-
            If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token.
             Ensure that either the roles are requested by scope or enable the user role assertion on the
             project.
             If not set, the id token role assertion will not be changed.
          nullable: true
        idTokenUserinfoAssertion:
          type: boolean
          title: id_token_userinfo_assertion
          description: |-
            If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes
             are added to the id token even if an access token is issued. This can be required by some applications
             that do not call the userinfo endpoint after authentication or directly use the id_token for retrieving
             user information.
             Attention: this violates the OIDC specification, which states that these claims must only be
             requested from the userinfo endpoint if an access token is issued. This is to prevent
             leaking of personal information in the id token, which is often stored in the browser and
             therefore more vulnerable.
             If not set, the id token userinfo assertion will not be changed.
          nullable: true
        clockSkew:
          title: clock_skew
          description: |-
            ClockSkew is used to compensate time differences between the servers of ZITADEL and the application.
             It is added to the "exp" claim and subtracted from "iat", "auth_time" and "nbf" claims.
             The default is 0s, the maximum is 5s.
             If not set, the clock skew will not be changed.
          nullable: true
          $ref: '#/components/schemas/google.protobuf.Duration'
        additionalOrigins:
          type: array
          items:
            type: string
          title: additional_origins
          description: |-
            AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used
             additional to the redirect_uris.
             This is useful if the application is used from an origin different to the redirect_uris,
             e.g. if the application is a SPA served in a native app, where the redirect_uri is a custom scheme,
             but the application is served from a https origin.
             Any existing additional origins not included in this list will be removed.
             If not set, the additional origins will not be changed.
        skipNativeAppSuccessPage:
          type: boolean
          title: skip_native_app_success_page
          description: |-
            For native apps a successful login usually shows a success page with a link to open the application again.
             SkipNativeAppSuccessPage can be used to skip this page and open the application directly.
             If not set, the skip native app success page will not be changed.
          nullable: true
        backChannelLogoutUri:
          type: string
          title: back_channel_logout_uri
          description: |-
            BackChannelLogoutURI is used to notify the application about terminated sessions according
             to the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).
             If not set, the back channel logout URI will not be changed.
          nullable: true
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: UpdateOIDCApplicationConfigurationRequest
      additionalProperties: false
    zitadel.application.v2.UpdateSAMLApplicationConfigurationRequest:
      type: object
      oneOf:
        - properties:
            metadataUrl:
              type: string
              title: metadata_url
          title: metadata_url
          required:
            - metadataUrl
        - properties:
            metadataXml:
              type: string
              title: metadata_xml
              format: byte
          title: metadata_xml
          required:
            - metadataXml
      properties:
        loginVersion:
          title: login_version
          description: |-
            LoginVersion specifies the login UI, where the user is redirected to for authentication.
             It can be used to select a specific login UI, e.g. for embedded UIs or for custom login pages
             hosted on any other domain.
             If unset, the login UI is chosen by the instance default.
          nullable: true
          $ref: '#/components/schemas/zitadel.application.v2.LoginVersion'
      title: UpdateSAMLApplicationConfigurationRequest
      additionalProperties: false
    zitadel.filter.v2.PaginationRequest:
      type: object
      properties:
        offset:
          type:
            - integer
            - string
          title: offset
          format: int64
          description: Starting point for retrieval, in combination of offset used to query a set list of objects.
        limit:
          type: integer
          title: limit
          description: |-
            limit is the maximum amount of objects returned. The default is set to 100
             with a maximum of 1000 in the runtime configuration.
             If the limit exceeds the maximum configured ZITADEL will throw an error.
             If no limit is present the default is taken.
        asc:
          type: boolean
          title: asc
          description: |-
            Asc is the sorting order. If true the list is sorted ascending, if false
             the list is sorted descending. The default is descending.
      title: PaginationRequest
      additionalProperties: false
    zitadel.filter.v2.PaginationResponse:
      type: object
      properties:
        totalResult:
          type:
            - integer
            - string
          title: total_result
          format: int64
          description: Absolute number of objects matching the query, regardless of applied limit.
        appliedLimit:
          type:
            - integer
            - string
          title: applied_limit
          format: int64
          description: Applied limit from query, defines maximum amount of objects per request, to compare if all objects are returned.
      title: PaginationResponse
      additionalProperties: false
    connect-protocol-version:
      type: number
      title: Connect-Protocol-Version
      enum:
        - 1
      description: Define the version of the Connect protocol
      const: 1
    connect-timeout-header:
      type: number
      title: Connect-Timeout-Ms
      description: Define the timeout, in ms
    connect.error:
      type: object
      properties:
        code:
          type: string
          examples:
            - not_found
          enum:
            - canceled
            - unknown
            - invalid_argument
            - deadline_exceeded
            - not_found
            - already_exists
            - permission_denied
            - resource_exhausted
            - failed_precondition
            - aborted
            - out_of_range
            - unimplemented
            - internal
            - unavailable
            - data_loss
            - unauthenticated
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        detail:
          $ref: '#/components/schemas/google.protobuf.Any'
      title: Connect Error
      additionalProperties: true
      description: 'Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation'
    google.protobuf.Any:
      type: object
      properties:
        type:
          type: string
        value:
          type: string
          format: binary
        debug:
          type: object
          additionalProperties: true
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
security: []
tags:
  - name: zitadel.application.v2.ApplicationService
    description: |-
      Service to manage applications.
       This service provides methods to create, update, delete and list application and application keys.

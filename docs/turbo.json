{
    "extends": ["//"],
    "tasks": {
      "generate": {
        "outputs": ["static/apis/**"],
        "inputs": ["../proto/**", "buf.gen.yaml"],
        "cache": true
      },
      "build": {
        "dependsOn": ["generate"],
        "outputs": ["build/**"],
        "cache": true
      }
    }
  }
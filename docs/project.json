{
  "name": "@zitadel/docs",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm build",
        "cwd": "docs"
      },
      "outputs": [
        "{projectRoot}/.next"
      ],
      "dependsOn": [
        "^build",
        "check-links"
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next dev",
        "cwd": "docs"
      },
      "dependsOn": [
        "generate"
      ]
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next start",
        "cwd": "docs"
      },
      "dependsOn": [
        "build"
      ]
    },
    "generate-buf": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm generate:buf",
        "cwd": "docs"
      },
      "outputs": [
        "{projectRoot}/openapi"
      ]
    },
    "clean-openapi": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm clean:openapi",
        "cwd": "docs"
      },
      "dependsOn": [
        "generate-buf"
      ],
      "outputs": [
        "{projectRoot}/openapi"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm clean",
        "cwd": "docs"
      }
    },
    "generate-openapi": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm generate:openapi",
        "cwd": "docs"
      },
      "outputs": [
        "{projectRoot}/content/docs/reference/api"
      ],
      "dependsOn": [
        "clean-openapi"
      ]
    },
    "generate-source": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec fumadocs-mdx",
        "cwd": "docs"
      },
      "outputs": [
        "{projectRoot}/.source"
      ],
      "dependsOn": [
        "generate-openapi"
      ]
    },
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Generation complete'",
        "cwd": "docs"
      },
      "dependsOn": [
        "generate-source"
      ]
    },
    "check-links": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm check-links",
        "cwd": "docs"
      },
      "dependsOn": [
        "generate"
      ]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint .",
        "cwd": "docs"
      }
    }
  }
}

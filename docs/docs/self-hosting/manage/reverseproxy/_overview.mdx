import CodeBlock from '@theme/CodeBlock';
import ZitadelYaml from "!!raw-loader!./zitadel.yaml";
import ComposeYaml from "!!raw-loader!./docker-compose.yaml";

<></>With these examples, you create and run a minimal <>{props.proxylink}</> configuration for ZITADEL with <a href="https://docs.docker.com/compose">Docker Compose</a>.
The base ZITADEL configuration is defined in the zitadel.yaml file and is bind-mounted to the container.
<details>
    <summary>zitadel.yaml</summary>
    <CodeBlock language="yaml">{ZitadelYaml}</CodeBlock>
</details>
You can either setup your environment for <a href={'#tls-mode-enabled'}>TLS mode enabled</a>, <a href={'#tls-mode-external'}>TLS mode external</a> or <a href={'#tls-mode-disabled'}>TLS mode disabled</a>.
For each mode, ZITADEL is configured a bit differently.
The base docker compose configuration enriches the ZITADEL configuration from above with environment variables for the different TLS mode.
<details>
    <summary>docker-compose.yaml</summary>
    <CodeBlock language="yaml">{ComposeYaml}</CodeBlock>
</details>

After you installed ZITADEL, you can log in using the following credentials:

- **username**: *zitadel-admin@<span></span>zitadel.127.0.0.1.sslip.io*
- **password**: *Password1!*


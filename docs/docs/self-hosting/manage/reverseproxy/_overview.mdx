import CodeBlock from '@theme/CodeBlock';
import ComposeYaml from "!!raw-loader!./docker-compose.yaml";

<>With these examples, you create and run a minimal {props.proxylink} configuration for ZITADEL with <a href="https://docs.docker.com/compose">Docker Compose</a>.
Whereas the guide focuses on the configuration for {props.proxylink}, you can inspect the configurations for ZITADEL and the database in the base Docker Compose file.</>
<details>
    <summary>base docker-compose.yaml</summary>
    <CodeBlock language="yaml">{ComposeYaml}</CodeBlock>
</details>

<>For running {props.proxylink}, you will extend the base Docker Compose file with the {props.proxylink} specific Docker Compose file.</>

<details>
    <summary>specific docker-compose.yaml</summary>
    <CodeBlock language="yaml">{props.compose}</CodeBlock>
</details>

You can either setup your environment for <a href={'#tls-mode-disabled'}>TLS mode disabled</a>, <a href={'#tls-mode-external'}>TLS mode external</a> or <a href={'#tls-mode-enabled'}>TLS mode enabled</a>.

After you installed ZITADEL, you can log in using the following credentials:

- **username**: *zitadel-admin@<span></span>zitadel.127.0.0.1.sslip.io*
- **password**: *Password1!*

<></>Neither <>{props.proxy}</> nor ZITADEL terminates TLS.
Nevertheless, <>{props.proxy}</> forwards unencrypted HTTP/2 traefik, aka h2c, to ZITADEL.
By executing the commands below, you will download the files necessary to run ZITADEL behind <>{props.proxy}</> with the following config:

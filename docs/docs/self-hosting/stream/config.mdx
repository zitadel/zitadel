---
title: Configure Streams
sidebar_label: Config
---

import CodeBlock from '@theme/CodeBlock';
import DefaultConfig from "!!raw-loader!./default_config.yaml";

The Zitadel config allows for fine-grained configuration of streams.
Stream configurations can be applied to individual groups of streams.
For example, single records can be emitted multiple times with different formats.

<details open>
    <summary>Default Config</summary>
    <CodeBlock language="yaml">{DefaultConfig}</CodeBlock>
</details>


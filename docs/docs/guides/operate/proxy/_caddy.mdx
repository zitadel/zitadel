## TLS mode external

```
:443
{
	reverse_proxy h2c://localhost:8080
	tls internal #only non production
}
```

## TLS mode enabled

```
:443
{
	reverse_proxy h2c://localhost:8080
	tls internal #only non production
}
```

## TLS mode disabled

```
auto_https: off
:80
{
	reverse_proxy h2c://localhost:8080
	tls internal #only non production
}
```

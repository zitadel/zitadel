Download the docker compose example configuration

```bash
wget https://docs.zitadel.com/docs/guides/installation/docker-compose.yml
```

Run the database and application containers

```bash
docker compose up --detach
```

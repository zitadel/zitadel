---
title: Environment Variables
---

import {
  SetEnvironment,
  EnvInterpolate,
  EnvCode,
} from "../../src/components/environment";
import EnvironmentContext from "../../src/utils/environment";
import { useContext } from "react";
import CodeBlock from "@theme/CodeBlock";

export const Instance = () => {
  const { instance } = useContext(EnvironmentContext);
  return <div>{instance}</div>;
};

Enter your environment variables which are prefilled in docs here or use the documentation links in your instance console.

<SetEnvironment />

## Demo:

<CodeBlock language="ts">console.log('clientId');</CodeBlock>

<EnvInterpolate>
  {"Hello, {clientId}! How are you? Take a look at my {instance}"}
</EnvInterpolate>

<EnvCode language="dart">
  {`
    var _busy = false;
    var _authenticated = false;
    var _username = '';
    final storage = const FlutterSecureStorage();
    final clientId = "{clientId}";
    `}
</EnvCode>

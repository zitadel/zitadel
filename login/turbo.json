{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test:integration:setup": {
      "with": ["dev"]
    },
    "test:acceptance:setup:dev": {
      "with": ["dev"]
    }
  }
}

{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "@zitadel/devcontainer",
  "targets": {
    "compose": {
      "description": "Run Docker Compose command",
      "command": "docker compose -p zitadel_devcontainer",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  }
}
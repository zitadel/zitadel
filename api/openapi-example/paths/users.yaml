x-zitadel-permissions: []

parameters:
  - $ref: "../components/parameters/limit.yaml"
  - $ref: "../components/parameters/position.yaml"

get:
  tags:
    - User
  summary: Search users
  description: "Search users inside an organization with their available data."
  operationId: searchUsers
  x-zitadel-permissions:
    - user.search
  responses:
    200:
      description: The users, what else
      content:
        application/json:
          schema:
            $ref: "../components/schemas/users.yaml"
    404:
      description: No user was found you muppet
      content:
        application/json:
          schema:
            $ref: "../components/schemas/problem.yaml"
    500:
      description: Unexpected error
      content:
        application/json:
          schema:
            $ref: "../components/schemas/problem.yaml"
FirstInstance:
  InstanceName: ZITADEL
  DefaultLanguage: en
  Org:
    Name: ZITADEL
    Human:
      UserName: zitadel-admin #will be suffixed by the domain from config (if it's not an email). for example: zitadel-admin -> zitadel-admin@domain.tld
      FirstName: ZITADEL
      LastName: Admin
      NickName:
      DisplayName:
      Email:
        Address: #uses the username if empty
        Verified: true
      PreferredLanguage: en
      Gender:
      Phone:
        Number:
        Verified:
      Password: Password1!
      PasswordChangeRequired: true

Log:
  Level: debug
  Formatter:
    Format: text

database:
  host: localhost
  port: 26257
  database: zitadel
  password: 
  maxOpenConns: 3
  options: 
  user:
    username: zitadel
    ssl:
      mode: disable
      rootCert: 
      cert:
      key:
  # MaxConnLifetime: 30m
  # MaxConnIdleTime: 30m

adminUser:
  username: root

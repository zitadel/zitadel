Log:
  Level: debug
  Formatter:
    Format: text

database:
  host: localhost
  port: 26257
  database: zitadel
  maxOpenConns: 3
  options: 
  user:
    username: zitadel
    password: 
    ssl:
      mode: disable
      rootCert: 
      cert:
      key:
  # MaxConnLifetime: 30m
  # MaxConnIdleTime: 30m

adminUser:
  username: root

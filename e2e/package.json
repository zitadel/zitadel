{
  "name": "zitadel-e2e",
  "version": "0.0.0",
  "scripts": {
    "open": "npx cypress open",
    "e2e": "npx cypress run --browser chrome",
    "open:dev": "CYPRESS_BASE_URL=http://localhost:4200 CYPRESS_BACKEND_URL=http://localhost:8080 npm run open -- --env grepTags='--@same-origin' --",
    "e2e:dev": "CYPRESS_BASE_URL=http://localhost:4200 CYPRESS_BACKEND_URL=http://localhost:8080 npm run e2e -- --env grepTags='--@same-origin' --",
    "lint": "prettier --check cypress",
    "lint:fix": "prettier --write cypress"
  },
  "private": true,
  "dependencies": {
    "@cypress/grep": "^3.1.3",
    "@types/chrome-remote-interface": "^0.31.8",
    "chrome-remote-interface": "^0.32.0",
    "cypress-otp": "^1.0.3",
    "cypress-wait-until": "^1.7.2",
    "mochawesome": "^7.1.3",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4",
    "uuid": "^9.0.0",
    "wait-on": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.8.3",
    "cypress": "^12.3.0"
  }
}
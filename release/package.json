{
  "name": "@zitadel/release",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Release tooling for ZITADEL",
  "scripts": {
    "test-unit": "vitest run --testNamePattern='Release Unit'",
    "test-e2e": "vitest run --testNamePattern='Release E2E'",
    "lint": "pnpm tsc --noEmit"
  },
  "dependencies": {
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/yargs": "^17.0.33",
    "typescript": "^5.7.3",
    "vitest": "^2.0.0"
  }
}

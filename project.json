{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "start": {
      "description": "Build and start the Zitadel platform (core, login and console) in production mode",
      "dependsOn": [
        "@zitadel/login:start"
      ]
    }
  }
}

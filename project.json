{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "build": {
      "description": "Build the Zitadel platform (api, login and console)",
      "dependsOn": [
        "@zitadel/api:build",
        "@zitadel/login:build"
      ]
    },
    "start": {
      "continuous": true,
      "description": "Start the the Zitadel platform (api, login and console)",
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run @zitadel/devcontainer:compose up --watch db api login"
      }
    },
    "build-and-start": {
      "continuous": true,
      "description": "Build and start the the Zitadel platform (api, login and console)",
      "dependsOn": [
        "build"
      ],
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run start"
        ]
      }      
    },
    "stop": {
      "description": "Stop all services",
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run @zitadel/devcontainer:compose stop db api login"
      }
    },
    "generate": {
      "description": "Generate everything needed to build the Zitadel platform (api, login and console)",
      "dependsOn": [
        "@zitadel/api:generate",
        "@zitadel/login:generate",
        "@zitadel/console:generate"
      ]
    },
    "clean": {
      "description": "Clean all local-only files relating to the Zitadel platform (api, login and console)",
      "dependsOn": [
        "@zitadel/api:clean",
        "@zitadel/login:clean",
        "@zitadel/console:clean"
      ]
    }
  }
}

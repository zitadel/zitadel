NEXT_PUBLIC_BASE_PATH=/ui/v2/login
EMAIL_VERIFICATION=false
ZITADEL_API_URL=http://localhost:8080
ZITADEL_SERVICE_USER_TOKEN_FILE=../../login-client.pat

# WebAuthn Relying Party ID override. Set this when the login UI is on a different domain than
# the Zitadel backend (e.g. login.company.com vs sso.company.com) and you want a shared parent
# domain (e.g. company.com) so passkeys registered here work across both. Must be a registrable
# domain suffix of the login UI's origin. Does NOT affect instance routing.
# ZITADEL_WEBAUTHN_RPID=company.com

# Whether ui_locales from OAuth requests should always override the language cookie.
# When set to "true", the language from ui_locales will always be used.
# When unset or set to "false", ui_locales only sets the language if no cookie exists (preserves user's manual selection).
# ZITADEL_UI_LOCALES_OVERRIDE_COOKIE=false

{
    "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
    "name": "@zitadel/login-api-mock",
    "targets": {
        "build": {
            "description": "Build the API mock server",
            "command": "nx run @zitadel/devcontainer:compose build login-api-mock"
        },
        "serve": {
            "description": "Start the API mock server",
            "continuous": true,
            "command": "nx run @zitadel/devcontainer:compose up --watch --force-recreate --renew-anon-volumes login-api-mock"
        },
        "down": {
            "description": "Stop the API mock server",
            "command": "nx run @zitadel/devcontainer:compose down login-api-mock"
        }
    }
}
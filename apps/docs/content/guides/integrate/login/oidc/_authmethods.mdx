import PKCE from "./authmethods/_pkce.mdx";
import Basic from "./authmethods/_basic.mdx";
import Implicit from "./authmethods/_implicit.mdx";
import PKCENative from "./authmethods/_pkcenative.mdx";
import JWTPrivateKey from "./authmethods/_jwtpk.mdx";

export default function AuthMethods(props) {
  const { Tab, Tabs } = props.components || {};
  return props.selected == "web" ? (
    <div>
      <Tabs
        groupId="auth-type"
        defaultIndex={0}
        items={["PKCE","Basic Auth","JWT with Private Key"]}
      >
        <Tab value="PKCE">
          <PKCE components={props.components}  />
        </Tab>
        <Tab value="Basic Auth">
          <Basic components={props.components}  />
        </Tab>
        <Tab value="JWT with Private Key">
          <JWTPrivateKey components={props.components}  />
        </Tab>
      </Tabs>
    </div>
  ) : props.selected == "native" ? (
    <Tabs
      groupId="auth-type"
      defaultIndex={0}
      items={["PKCE"]}
    >
      <Tab value="PKCE">
        <PKCE components={props.components} Native />
      </Tab>
    </Tabs>
  ) : props.selected == "spa" ? (
    <Tabs
      groupId="auth-type"
      defaultIndex={0}
      items={["PKCE","Implicit"]}
    >
      <Tab value="PKCE">
        <PKCE components={props.components}  />
      </Tab>
      <Tab value="Implicit">
        <Implicit components={props.components}  />
      </Tab>
    </Tabs>
  ) : null;
}

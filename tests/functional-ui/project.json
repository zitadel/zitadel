{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "test-setup": {
        "dependsOn": [
            "test-state",
            "test-build",
            "@zitadel/api:build"
        ],
        "executor": "nx:run-commands",
        "options": {
            "env": {
                "API_CONFIG_FILE": "{projectRoot}/api.yaml"
            },
            "parallel": false,
            "commands": [
                "nx run @zitadel/devcontainer:compose up --detach --force-recreate --renew-anon-volumes db-functional-ui",
                "nx run @zitadel/api:setup --excludeTaskDependencies"
            ]
        }  
    },
    "test-env": {
        "continuous": true,
        "dependsOn": [
            "test-setup"
        ],
        "options": {
            "env": {
                "API_CONFIG_FILE": "{projectRoot}/api.yaml"
            }
        },
        "command": "nx run @zitadel/api:prod --excludeTaskDependencies"
    },
    "test-firefox": {
      "description": "Runs the functional UI tests against a Zitadel API and a Firefox browser",
      "dependsOn": [
        "test-env"
      ],
      "command": "pnpm cypress run --browser firefox --reporter-options reportDir=cypress/results/firefox --config videosFolder=cypress/videos/firefox,screenshotsFolder=cypress/screenshots/firefox",
      "options": {
        "parallel": false,
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": [
        "default"
      ]
    },
    "test-chrome": {
      "description": "Runs the functional UI tests against a Zitadel API and a Chrome browser.",
      "dependsOn": [
        "test-env"
      ],
      "command": "pnpm cypress run --browser chrome --reporter-options reportDir=cypress/results/chrome --config videosFolder=cypress/videos/chrome,screenshotsFolder=cypress/screenshots/chrome",
      "options": {
        "parallel": false,
        "cwd": "{projectRoot}"
      },
      "cache": true,
      "inputs": [
        "default"
      ]
    }
  }
}
